zip:

unzip -l filename

解凍せずファイルの中身確認できる

tar:

tar tvf fileName

t: ファイルの内容をリスト表示
v: 詳細を表示
f: ファイル名を指定

解凍
tar jxf ファイル名.tar.bz2 -C 解凍先ディレクトリ

j: bzip2 形式で圧縮されたアーカイブを指定
x: ファイルを展開
f: ファイル名を指定

削除:
    find . -type f -delete
    ファイルをすべて削除(カレントディレクトリ)

圧縮系
k: 元ファイルを残す
    gzip -k pgadmin4-8.9-x64.exe
    bzip2 -k pgadmin4-8.9-x64.exe
    xz -k pgadmin4-8.9-x64.exe